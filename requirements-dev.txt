# Install production deps first
-r requirements.txt

# Dataset generation (run once during setup, not needed at runtime)
faker==33.1.0
pandas==2.2.3
numpy==1.26.4

# Document / media text extraction (lazily imported in media/service.py — not
# needed on the server unless users upload PDFs/DOCX/PPTX and you want indexing)
boto3==1.35.68
PyMuPDF>=1.24.0
python-docx>=1.1.0
python-pptx>=1.0.0

# LangChain community integrations (not used in production code)
langchain-community==0.3.7

# PII detection (pii_guard.py falls back to regex if absent)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Local LLM fallback — Flan-T5 Small requires PyTorch which is ~2 GB.
# Cannot run on Render free tier (512 MB). The fallback gracefully returns
# an extractive stub when transformers/torch is absent.
transformers>=4.40.0
sentencepiece>=0.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# Load testing
locust==2.32.4

# Evaluation framework — pulls in PyTorch via sentence-transformers (~2 GB).
# NEVER add to requirements.txt; free-tier builds will OOM.
deepeval>=1.4.0
