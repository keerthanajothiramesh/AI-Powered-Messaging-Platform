# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
python-multipart==0.0.12

# Database
sqlalchemy==2.0.35
alembic==1.14.0
asyncpg==0.30.0
motor==3.6.0
pymongo==4.9.2

# Vector Store (pgvector on Neon PostgreSQL)
pgvector>=0.3.0

# Local Embeddings — fastembed uses ONNX (~50MB RAM, no rate limits, free-tier safe)
fastembed>=0.3.6

# AI / LLM
google-generativeai==0.8.3
rank-bm25==0.2.2

# Language detection
langdetect==1.0.9

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
google-auth==2.37.0

# Media + Document text extraction (lazy-imported, only loads when file is uploaded)
boto3==1.35.68
Pillow==11.0.0
PyMuPDF>=1.24.0
python-docx>=1.1.0
python-pptx>=1.0.0

# PII detection — regex-only on free tier (no spaCy/Presidio, saves ~300MB RAM)
# To enable full Presidio NER locally: pip install presidio-analyzer presidio-anonymizer spacy
#   and: python -m spacy download en_core_web_sm

# Observability
structlog==24.4.0

# Utilities
pydantic[email]==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0
httpx==0.28.0
python-dotenv==1.0.1
tenacity==8.4.2

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
